spring:
  cloud:
    config:
      auto-refresh: true
      refresh-interval: 60
    gateway:
      routes:
        - id: httpbin
          uri: http://httpbin.org:80
          predicates:
          - Path=/get
        - id: randomuser-test
          uri: https://poc-app-poc-app-service.azuremicroservices.io
          predicates:
          - Path=/randomuser
        - id: vault-access-test
          uri: https://poc-app-poc-app-service.azuremicroservices.io
          predicates:
          - Path=/randomuser
        - id: config-access-test
          uri: https://poc-app-poc-app-service.azuremicroservices.io
          predicates:
          - Path=/testproperty

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: refresh

---

spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: info
    org.springframework.web: info
    org.springframework.security: info
    org.springframework.cloud.netflix: error
    com.netflix.discovery: error
    org.nahsi.example.test.scheduler: debug

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
        exclude: "env,beans"

---