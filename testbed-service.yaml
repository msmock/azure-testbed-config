
testProperty: test property setting

spring:
    cloud:
         config:
          auto-refresh: true
          refresh-interval: 60

---

spring:
  config:
    activate:
      on-profile: dev
logging:
  level:
    root: info
    org.springframework.web: info
    org.springframework.security: info
    org.nahsi.example.test.config: info

---
